import { render } from "@testing-library/react"
import Overlay from "../Overlay"

describe('overlay component', () => {
    it('renders overlay component successfully', () => {
        render(<Overlay />)
    })
})