
import { render } from "@testing-library/react";
import MoviePoster, { defaultProps } from "../MoviePoster";
import userEvent from "@testing-library/user-event";

describe('movie poster component', () => {
    test('like count increased', () => {
        // const component = render(<MoviePoster movieDetails={{id:'', name:'', likes:'0', description:'', actors:[''], isLiked:false}} updateLike={()=>{}} updateSelectedMovie={()=>{}} selectMovie={()=>{}}/>)
        // const likeComponent = component.findAllByRole('svg');


    });
})